# Render 部署配置
services:
  - type: web
    name: fayan-ai
    env: docker
    plan: free
    dockerfilePath: ./Dockerfile
    envVars:
      - key: MINIMAX_API_KEY
        sync: false
    healthCheckPath: /api/agent/health
    autoDeploy: true
